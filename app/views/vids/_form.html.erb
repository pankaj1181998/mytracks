<%= form_with(model: vid, local: true) do |form| %>
  <% if vid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vid.errors.count, "error") %> prohibited this vid from being saved:</h2>

      <ul>
      <% vid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :vid_link %>
    <%= form.text_field :vid_link %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
